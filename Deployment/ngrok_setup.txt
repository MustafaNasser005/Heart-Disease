# Heart Disease Prediction System - Deployment Guide

## 🚀 Deploying with ngrok

This guide will help you deploy your Streamlit Heart Disease Prediction app using ngrok for external access.

## 📋 Prerequisites

1. **Python Environment**: Ensure your app runs locally first
2. **ngrok Account**: Sign up at https://ngrok.com/
3. **ngrok CLI**: Download and install ngrok

## 🔧 Setup Instructions

### Step 1: Install ngrok

#### Windows:
1. Download ngrok from https://ngrok.com/download
2. Extract the zip file
3. Add ngrok to your PATH or place it in your project directory

#### macOS/Linux:
```bash
# Using Homebrew (macOS)
brew install ngrok

# Using Snap (Linux)
sudo snap install ngrok
```

### Step 2: Authenticate ngrok

1. Sign up at https://ngrok.com/
2. Get your authtoken from the dashboard
3. Run authentication:
```bash
ngrok config add-authtoken YOUR_AUTHTOKEN_HERE
```

### Step 3: Start Your Streamlit App

```bash
# Navigate to your project directory
cd "Heart Disease"

# Start the Streamlit app
cd UI
streamlit run app.py
```

Your app should now be running at `http://localhost:8501`

### Step 4: Expose with ngrok

Open a new terminal window and run:

```bash
# Expose the Streamlit app
ngrok http 8501
```

### Step 5: Access Your App

ngrok will provide you with a public URL like:
```
Forwarding    https://abc123.ngrok.io -> http://localhost:8501
```

Share this URL with others to access your app from anywhere!

## 🔒 Security Considerations

### For Production Use:
1. **Authentication**: Consider adding login requirements
2. **HTTPS**: ngrok provides HTTPS by default
3. **Rate Limiting**: Monitor usage to prevent abuse
4. **Data Privacy**: Ensure patient data is handled securely

### Environment Variables:
```bash
# Set environment variables for production
export STREAMLIT_SERVER_PORT=8501
export STREAMLIT_SERVER_ADDRESS=0.0.0.0
```

## 🛠️ Troubleshooting

### Common Issues:

1. **Port Already in Use**:
   ```bash
   # Kill process using port 8501
   lsof -ti:8501 | xargs kill -9
   ```

2. **ngrok Not Found**:
   ```bash
   # Ensure ngrok is in PATH or use full path
   ./ngrok http 8501
   ```

3. **Authentication Failed**:
   ```bash
   # Re-authenticate ngrok
   ngrok config add-authtoken YOUR_AUTHTOKEN_HERE
   ```

4. **App Not Loading**:
   - Check if Streamlit is running on port 8501
   - Verify all dependencies are installed
   - Check the ngrok tunnel status

## 📊 Monitoring

### ngrok Dashboard:
- Visit https://dashboard.ngrok.com/
- Monitor traffic and usage
- View request logs

### Streamlit Logs:
```bash
# View Streamlit logs
tail -f ~/.streamlit/logs/streamlit.log
```

## 🔄 Alternative Deployment Options

### 1. Streamlit Cloud
- Deploy directly to Streamlit Cloud
- Free tier available
- Automatic HTTPS

### 2. Heroku
- Deploy to Heroku platform
- Requires Procfile and requirements.txt
- Free tier discontinued

### 3. AWS/GCP/Azure
- Deploy to cloud platforms
- More control and scalability
- Requires additional setup

## 📝 Configuration Files

### streamlit_config.toml (Optional)
Create `~/.streamlit/config.toml`:
```toml
[server]
port = 8501
address = "0.0.0.0"
enableCORS = false
enableXsrfProtection = false

[browser]
gatherUsageStats = false
```

## 🎯 Best Practices

1. **Testing**: Always test locally before deploying
2. **Backup**: Keep local copies of your models and data
3. **Monitoring**: Monitor app performance and usage
4. **Updates**: Keep dependencies updated
5. **Documentation**: Maintain clear deployment documentation

## 📞 Support

If you encounter issues:
1. Check ngrok status: https://status.ngrok.com/
2. Review Streamlit documentation: https://docs.streamlit.io/
3. Check the app logs for error messages
4. Verify all file paths and dependencies

---

**Note**: This deployment guide is for educational purposes. For production medical applications, ensure compliance with healthcare regulations and data privacy laws.

